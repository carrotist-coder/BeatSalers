{
  "name": "beatsalers",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm run dev --prefix server\" \"npm start --prefix client\"",
    "test": "npm run test --prefix server && npm run test --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/carrotist-coder/BeatSalers.git"
  },
  "private": true
}
